server:
  port: 8086
#  servlet:
#    context-path: /MSC

spring:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/online_store
    username: root
    password: m79erXrA

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/
